package hello;

public class ternary {
	public static void main(String[]args) {
	    int x=22,y=10,z=90;
 	   String a =  ((x>y) && (x>z)) ? "X is Greater" : (y>z) && (y>x) ? "Y is Greater" :"Z is Greater";

	}

}
